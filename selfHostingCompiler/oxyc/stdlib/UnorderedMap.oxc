#include "Vector.oxc"

class UnorderedMap
{
	Vector keys;
	Vector values;

	this()
	{
		this->keys();
		this->values();
	}

	~this()
	{
		this->keys.~this();
		this->values.~this();
	}

	void put(void* key, void* value)
	{
		keys.add(key);
		values.add(value);
	}

	void* get(void* key)
	{
		
	}
};