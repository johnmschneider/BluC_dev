0) Todo list
0-1) implement macros
0-2) handle #elif directive
0-3) handle #else directive
0-4) add support for multiline #define's (aka \ character)
0-5) implement default string type being string class
0-6) (parseTree.txt) new class begins at line 1042      // <<<< what is this? i forget

0-7) change all references to "filepath, filePath, filename, fileName" to
    just "filePath"

0-8) refactor out nullSafeEquals because java's Objects.equals (NOT Object.equals)
    checks for null

0-9) refactor Expression.Literal to use the Operand field from Expression instead
    of its own unique "value" field

0-10) double check all of the custom .equals methods to see if they need
    refactoring